<ul class="row">
  
        {{#each usersGreeted}}
        <li><a href="counter/{{names}}">{{names}}</a> has been greeted</li>
        {{/each}}
      </ul>
      <div class="error">
          {{messages.error}}
        </div>
    